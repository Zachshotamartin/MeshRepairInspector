Guided repair update: eight examples, plain-language issue/repair explanations, relevant default operations, actual before/after counts and a glossary. Inspection views expose changes that retain a silhouette: disconnected panels spread apart, normal arrows show reversed faces, and thick edge markers identify junctions. Display offsets do not affect exports.

New loose-fin cleanup removes only an unambiguous single-face appendage; it does not attempt a general nonmanifold-solid repair. User imports do not automatically enable destructive component removal or hole filling. Invalid imports preserve working geometry.

Validation: unit tests cover parsing bounds, connectivity, hole caps and unsupported boundaries, orientation, conservative fin removal, all guided repair pipelines, and diagnostic regions. The Chromium suite applies every example's operations, parses actual downloaded OBJ geometry, checks report counts and visual changes, restores the original and undo state, and exercises uploads and 390px layout. No GitHub Actions added.
